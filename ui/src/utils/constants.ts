const THEME_STORAGE_KEY = "theme";

const VIEWER_MODES = {
  ANNOTATE: "annotate",
  VIEW: "view",
} as const;

const NEW_ANNOTATION_SET_ID = "new-annotation-set";

export { THEME_STORAGE_KEY, VIEWER_MODES, NEW_ANNOTATION_SET_ID };
