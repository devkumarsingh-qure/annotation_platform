const DEFAULT_PAGE_SIZE = 5;

const VIEWER_MODES = {
    ANNOTATE: "annotate",
    VIEW: "view",
} as const;

const NEW_ANNOTATION_SET_ID = "new-annotation-set";

export {
    DEFAULT_PAGE_SIZE,
    VIEWER_MODES,
    NEW_ANNOTATION_SET_ID,
};